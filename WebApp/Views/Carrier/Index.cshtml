@model IEnumerable<Entities.Models.tbCarrier>

@{
    ViewBag.Title = "Carrier";
}

<div class="box-body">
    @if (TempData["Exists"] != null && (bool)TempData["Exists"])
    {
        <div class="divmsg">
            <div class="alert alert-success" role="alert">
                @TempData["Message"].ToString()
            </div>
        </div>
    }

    <div style="padding-bottom:10px;">
        <a href="@Url.Content("~/Carrier/New")" class="btn btn-success "><i class="glyphicon glyphicon-plus"></i>&nbsp;New Carrier</a>
    </div>
    <div id="Grid" class="table-responsive">
        @if (Model.Count() == 0)
        {
            <div class="text-center" style="padding-top: 20px; display: block;"><span class="label label-danger">No carrier registered</span></div>
        }
        else
        {
            <table class="table table-hover table-highlight-head grid">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)   
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NickName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Status)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NickName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Status)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </table>
        }
    </div>
</div>



